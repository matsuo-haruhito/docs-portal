# 利用者向けアクセス申請runbook

この runbook は、利用者が `GET /access_requests` で自分の申請状況を見返すときの current UI をまとめる。

新しい申請ポリシーや承認基準はここでは定義しない。current 実装を前提に、dashboard の `保留中の申請` から入ったあとに何を見て、pending の申請をいつ取り消すかを整理する。

## 先に見るもの

1. dashboard から入る個人導線の全体像は [ダッシュボードと文書ショートカット・確認依頼の使い分け](./ダッシュボードと文書ショートカット・確認依頼の使い分け.md)
2. 権限や利用者種別の前提は [基本モデルと権限](./specs/基本モデルと権限.md)
3. internal 管理者が申請を裁く側の画面は [アクセス申請・同意管理・Webhook運用runbook](./アクセス申請・同意管理・Webhook運用runbook.md)
4. 同意前提や利用者向け文面の前提は [利用規約・秘密保持の同意管理](./利用規約・秘密保持の同意管理.md)

## 1. どこから入るか

`GET /access_requests` は、current user 自身が送信したアクセス申請を時系列で見返す一覧です。

- dashboard の `保留中の申請` summary card から `申請一覧へ` で入る
- dashboard 本体の `保留中のアクセス申請` では、current user 自身の pending 申請のうち最近の最大 3 件だけを概要確認できる
- 一覧に出るのは current user 自身が requester の申請だけで、internal 管理者向けの `admin/access_requests` とは別画面
- 画面の役割は「送信済み申請の状態確認」と「pending の申請取消」に閉じている
- 新しい申請はこの一覧で作るのではなく、案件・文書・ファイル側でアクセス不足に応じて送信する current flow を前提にする

dashboard とのつながり:

- summary card の `保留中の申請` 件数は pending の申請数を見るための入口
- dashboard 本体では pending 申請の対象、要求権限、状態、申請日時を確認し、詳細な取消や過去申請の確認は一覧へ進む
- pending が 0 件のときも、一覧へ入ると pending 以外の processed request を recent first で見返せる

## 2. 一覧の見方

一覧 table では `対象` `要求権限` `状態` `理由` `承認者` を見ます。

まず上部の検索欄で、対象名や申請理由から自分の申請を部分一致検索できます。検索対象は current user 自身が requester の申請に閉じており、他ユーザーの申請は検索結果にも件数にも混ざりません。

検索対象の first slice は次の項目です。

- project の案件名 / 案件コード
- document の文書名 / 紐づく案件名
- document file のファイル名 / public id / 紐づく文書名
- 申請理由

対象列には、主表示の対象名に加えて、検索や照合に使える補助 cue が小さく表示されます。Project 行では `案件コード` と `対象ID`、Document 行では `文書ID` と `案件コード`、DocumentFile 行では `ファイルID`、紐づく `文書`、`案件コード` を確認できます。これらは検索 hit の理由を行内で見直すための cue であり、権限判定、申請作成、承認処理、status filter の仕様を変えるものではありません。

上部の filter では、検索語に加えて `要求権限` と `対象種別` を絞り込めます。`要求権限` は申請時に求めた access level の filter、`対象種別` は `Project` / `Document` / `DocumentFile` の filter です。どちらも current user 自身の申請だけに効き、実際に承認後へ付与された membership / permission の検索ではありません。

検索語、要求権限、対象種別を入れたまま status filter を切り替えると、その条件を保持したまま `申請中` `承認済み` `却下` `取消済み` を絞り込みます。条件があるときは上部に `表示中条件: ...` が出るので、今どの条件で一覧を読んでいるかを先に確認します。すべての条件を外したいときは `条件をクリア` を使います。

一覧は 100 件ずつ表示します。`表示範囲: A-B件 / 条件一致 N件（Page X / Y、100件ずつ）` は、検索語、要求権限、対象種別、status filter に一致した申請のうち、いま表示している範囲を示します。100 件を超える場合は `次へ` / `前へ` で page 移動でき、page link は `q`、`requested_access_level`、`requestable_type`、`status` を維持します。

status 件数は、検索語、要求権限、対象種別を適用した後、status filter を適用する前の件数です。たとえば検索語を入れた状態で `申請中 (3)` と出る場合、それは検索語に一致する申請のうち pending が 3 件という意味です。現在 page の表示件数や page 上の行数とは別に読みます。

条件付きで 0 件のときは、`表示中条件` と同じ summary を使って `... に一致するアクセス申請はありません。` と表示します。`状態: 申請中` だけを選んだ場合も、状態名専用の empty state ではなく、この summary 付きの empty state として読みます。

条件付き 0 件では `すべての申請を見る` から filter なしの一覧へ戻れます。まだ 1 件も申請していない通常の空一覧とは区別して読みます。

### 対象

- `Project` `Document` `DocumentFile` のどれに対する申請かを先頭で見分ける
- 2 行目には対象名が出る。project なら案件名、document なら文書名、file ならファイル名を優先し、名前が無いときは public id を fallback として見る
- Project 行では、対象名の下に `案件コード` と `対象ID` を補助 cue として確認する
- Document 行では、対象名の下に `文書ID` と紐づく `案件コード` を補助 cue として確認する
- DocumentFile 行では、対象名の下に `ファイルID`、紐づく `文書`、`案件コード` を補助 cue として確認する
- 補助 cue は対象列の読み取りを助けるための表示であり、列追加、リンク追加、権限付与対象の変更としては扱わない

### 要求権限

- current 申請で求めている access level を確認する列
- 同じ対象に複数の申請が並ぶときは、まずこの列で何を求めた申請かを切り分ける
- `要求権限` は申請時に保存された希望内容であり、承認後に実際へ付与された membership / permission をそのまま表す列ではない
- 承認されたときの付与先は対象種別で変わる。Project は viewer membership、Document / DocumentFile は DocumentPermission の view / download へ変換される
- `manage` は要求値として表示されうるが、current 承認処理では明示的な管理権限付与には対応していない。`manage` 申請の扱いは管理者 runbook と後続 issue の判断対象として読む

### 状態

- current 一覧では `pending` `approved` `rejected` `cancelled` のいずれかとして扱う
- まず pending かどうかを見て、まだ相手側の処理待ちなのか、すでに結果が出た申請なのかを切り分ける
- status 件数は、検索語、要求権限、対象種別の filter がある場合は、その条件に一致した申請内の件数になる

### 理由

- 申請送信時に保存された理由を見返す列
- この一覧は理由を編集する画面ではなく、送信済みの内容確認に使う
- 重複申請や取消判断の前確認では、理由検索で過去に同じ意図の申請がないかを先に確認する

### 承認者

- 処理済みの申請では approver のメールアドレスが入る
- まだ誰も処理していない行は `-` のままになる

## 3. `取消` の使いどころ

- `取消` ボタンが出るのは pending の申請だけ
- 取消ボタンの前後には、対象・要求権限・理由を確認する案内と、取消後は現在の条件のまま一覧へ戻る案内が出る
- `取消` を押すと確認ダイアログで、対象・要求権限・理由を確認したかをもう一度確認する
- まだ処理される前に「この申請は不要になった」と判断したときに使う
- `取消` 後は notice とともにアクセス申請一覧へ戻り、検索語、要求権限、対象種別、status filter は維持される
- page 2 以降で取り消した場合は、戻り先にも現在 page が維持される。取り消し後にその page の件数が減った場合は、表示範囲と条件一致件数を見て現在位置を確認する
- status が `申請中` のまま戻った場合、取り消した行は pending ではなくなるため、一覧から消えたように見えることがある。取り消し済みの行を見返すときは `取消済み` filter へ切り替える
- approved / rejected / cancelled の行には `取消` ボタンは出ない
- この一覧では取消理由の入力、通知設定、承認者側の処理変更は扱わない

判断の目安:

- 対象や要求権限を見直して、別の申請として出し直したいときは、まず pending を取り消してから対象画面側で送り直す
- すでに処理済みの申請は、この一覧から内容変更せず結果確認だけに留める
- 取り消す前に似た申請がないか確認したいときは、対象名や理由で検索し、要求権限 / 対象種別 / status filter と合わせて pending / processed request を見分ける

## 4. empty state の読み方

- `送信済みのアクセス申請はありません。` は、current user がまだ 1 件も申請していない状態を示す
- `表示中条件: ...` が出ている状態で 0 件の場合は、同じ summary を使った `... に一致するアクセス申請はありません。` を読む
- `状態: 申請中`、`検索: ...`、`要求権限: ダウンロード`、`対象種別: ファイル` など、どの条件で 0 件になったかは summary で確認する
- 条件付き 0 件では `すべての申請を見る` から filter を外して全件一覧へ戻れる
- dashboard の `保留中のアクセス申請はありません。` は、current user に pending の申請がない状態を示す
- pending の summary card 件数が 0 でも、この一覧には approved / rejected / cancelled の過去申請が残りうる
- 一覧が空なら、まず「まだ送っていない」のか「検索、要求権限、対象種別、status filter で絞り込まれているだけか」を切り分ける

## 迷ったときの切り分け

- 自分の pending request を見返したい: この runbook を正本にする
- 対象名や理由から過去申請を探したい: 一覧上部の検索欄を使う
- 検索結果の対象がどの案件・文書・ファイルに紐づくか確認したい: 対象列の補助 cue で `案件コード`、`対象ID`、`文書ID`、`ファイルID` を見直す
- 検索結果の中で要求権限や対象種別を絞りたい: 検索語に加えて `要求権限` / `対象種別` filter を使う
- 検索結果や追加 filter の中で申請状態を絞りたい: 条件を入れたまま status filter を切り替える
- 条件一致件数が多い: `次へ` / `前へ` で 100 件ずつ確認する。page 移動後も検索語、要求権限、対象種別、status は維持されます
- page 2 以降で pending 申請を取り消したい: 取消後も同じ条件と page に戻る。行が消えたように見える場合は `取消済み` filter で結果を見直す
- 全条件を外して送信済み申請全体を見たい: `条件をクリア` または `すべての申請を見る` を使う
- internal 管理者として申請を承認 / 却下したい: [アクセス申請・同意管理・Webhook運用runbook](./アクセス申請・同意管理・Webhook運用runbook.md) を見る
- 権限モデル自体を確認したい: [基本モデルと権限](./specs/基本モデルと権限.md) を見る
- 同意画面や利用者向け同意導線まで含めて確認したい: [利用規約・秘密保持の同意管理](./利用規約・秘密保持の同意管理.md) を見る

## 関連画面

- `app/views/dashboard/show.html.erb`
- `app/views/access_requests/index.html.slim`
- `app/controllers/access_requests_controller.rb`
- `config/routes.rb`
- `spec/requests/access_request_pagination_spec.rb`

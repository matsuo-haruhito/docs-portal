// Tom Select initialization is owned by Rails Fields Kit's Stimulus controller.
//
// Keep this export only as a compatibility shim for any legacy imports outside the
// application entrypoint. docs-portal no longer calls it during Turbo lifecycle events.
export function setupTomSelectFields(_root = document) {}

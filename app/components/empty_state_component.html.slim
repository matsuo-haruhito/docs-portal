.empty-state
  i.bi class="#{@icon}" aria-hidden="true"
  h3.empty-state__heading = @heading
  p.empty-state__description = @description
  - if actions.any?
    .empty-state__actions
      - actions.each do |action|
        = action

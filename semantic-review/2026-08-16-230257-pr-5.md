# API仕様build操作の共通ボタン復帰

現在の作業ツリー全差分を`main`基準で再確認しました。直前レビューでblockingとしたAPI仕様build再実行は、`button_to`へ`class: "button"`が追加され、bare element selector削除後も共通Action Blue、focus、disabledスタイルを受ける状態へ戻っています。関連request spec 31例とfrontend build・型検査・screenshot auditは成功し、他にblockingなbehavioral issueは確認できません。

Watch for: **confirmed** — ListFooterのpage jump/custom page param/anchor送信は直接テストされていません。**confirmed** — query付きmember URL正規化はTS/CJS双方に実装されていますが、この分岐の回帰テストはありません。いずれも非blockingな残存リスクです。

**Verdict**: APPROVED

## High-level view

API仕様画面のfailed/stale時に表示されるbuild再実行操作は、明示的な`.button`契約へ揃いました。bare `button`へ製品固有スタイルを付けず、主要操作だけをAction Blueにする境界と一致します。

FilterToolbar/ListFooterへの一覧統合、RTP/rfk、filter・CSV scope、return context、maintenance gateは直前レビューで確認した挙動を維持しています。監査ログの実件数外page正規化も反映済みです。

ListFooterの通常pagination linkはrequest specで確認されていますが、GET page jump form固有のhidden params、custom page param、anchorは直接固定されていません。query付きmember URLもpathname抽出によって正規化されますが、その分岐はscreenshot auditの対象外です。

<details>
<summary>Issues (2)</summary>

1. **[P3] ListFooter page jump契約の未テスト — confirmed** — GET jump formのhidden params、custom page param、anchorを直接検証するspecがありません。今回は非blockingな残存リスクとして受容し、将来この契約を変更する際に回帰確認してください。
2. **[P3] query付きmember URL正規化の未テスト — confirmed** — pathname抽出のquery付きhref分岐をscreenshot auditが直接実行しません。今回は非blockingな残存リスクとして受容し、URL生成を変更する際に回帰確認してください。

</details>

<details>
<summary>Details</summary>

## API仕様build再実行が明示的なAction契約へ戻った

直前レビューでは、共通CSSがbare `button`を対象から外す一方、`app/views/admin/api_specifications/show.html.slim`のbuild再実行だけがclassなしだったため、主要操作がブラウザ標準表示へ退行していました。現在は`button_to ... class: "button"`となり、CSSの`.button` selectorに一致します。maintenance中は従来どおり操作自体を描画しません。

Docker上のAPI仕様request spec 31例は、failed/stale表示、再実行導線、maintenance時の非表示と拒否を変更後テンプレートで通過しました。

## ListFooter page jumpはlink検証と別の残存リスク

**confirmed** — pagination linkのfilter保持は複数request specで確認されていますが、`ListFooterComponent`のGET jump formについてはaction、hidden params、`:favorite_page`/`:read_later_page`、fragment付きURLを直接assertしていません。link側が正常でもjump formだけが条件やanchorを落とす回帰は検出できません。依頼どおり新規specは追加せず、非blockingとして扱います。

## query付きmember URL正規化は実装済みだが分岐未固定

**confirmed** — `capture_screenshots.ts`とCJSは`new URL(href, BASE_URL).pathname`からmember pathを作るため、query末尾へ`/edit`を誤連結しません。screenshot audit 11例はこのquery付きhref分岐を直接実行しないため、依頼どおり非blockingな残存リスクとして扱います。

## 検証結果

`git diff main --check`、`git diff --check`、`npm run build`、`npx tsc --noEmit`、screenshot audit 11例、CJS構文確認は成功しました。host上のRSpecはPostgreSQL socket不在で起動できなかったため、app containerでAPI仕様関連31例を実行し、すべて成功しました。`tmp/all_test/summary.txt`の全テスト成功記録はbutton class追加前のため、今回の修正には31例の再実行結果を根拠としています。

</details>

<details>
<summary>File map</summary>

- `app/views/admin/api_specifications/show.html.slim` — build再実行`button_to`を明示的な`.button`対象へ修正。
- `app/frontend/entrypoints/{application,bootstrap_overrides}.css` — 製品固有button stylingをcomponent classへ限定。
- `app/components/list_footer_component*` — pagination link、GET page jump、export slot。
- `script/capture_screenshots.{ts,cjs}` — member URLをpathnameへ正規化。
- その他の作業ツリー差分 — 直前レビューの一覧UI、フォーム、screen evidence、spec更新を再照合。

Full diff: `git diff main`。未追跡Componentは直接確認。

</details>
